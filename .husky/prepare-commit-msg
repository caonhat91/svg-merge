#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "WARNING: prepare-commit-msg hook, validate msg"
COMMIT_MSG_FILE=$1
COMMIT_SOURCE=$2

    msgHint=$(cat "$COMMIT_MSG_FILE")
    echo "$msgHint"
