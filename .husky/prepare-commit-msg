#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "WARNING: prepare-commit-msg hook, validate msg"
COMMIT_MSG_FILE=$1
COMMIT_SOURCE=$2

if [[ -z "$COMMIT_SOURCE" ]]
then
    msgHint=$(cat "$COMMIT_MSG_FILE")
    echo "$msgHint"
fi
