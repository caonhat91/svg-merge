#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "WARNING: prepare-commit-msg hook, validate msg"
COMMIT_MSG_FILE=$1
COMMIT_SOURCE=$2
SHA1=$3

echo "$1, $2, $3, $4, $COMMIT_MSG_FILE, $COMMIT_SOURCE"
